!!!still in development!!!
based on django 2.0.7


* make sure you have pip, virtualenv installed


* if you run the app for the first time you need to install django 2.0.7(it's in requirements.txt)
* to run the app try runscripts or follow the instructions bellow
* manual run:

* preparing the virtual environment

mkvirtualenv django_project --python=/usr/bin/python3.6
pip install -r requirements.txt

* run the server

cd src/
./manage.py runserver
