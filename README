!!!still in development!!!
based on django 2.0.7


* make sure you have pip, virtualenv installed


* if you run the app for the first time you need to install django 2.0.7
* to run the app try runscripts or follow the instructions bellow
* manual run:

pip install -r requirements

* to run the app on Unix/Linux:
* activate virtualenv

source bin/activate

* run the server

cd src/
python manage.py runserver
